
# Excel files
*.xlsx

# Virtual environment
venv/

# local Environment files
*.env
*.env.*
*.env_*
.env_*
# ...but keep the template committed so others know what to fill in
!.env.example

# Credentials
credentials.json
*.json
# ...but keep the template committed so others know what to fill in
!credentials.json.example

# CSE / image cache (raw API responses + downloaded images)
cache/
cache_full/
cache_prod/
cache_val/

# Pipeline output (final xlsx + downloaded images)
output/
output_full/
output_prod/
output_val/

# Photos
*.jpg
*.jpeg
*.png

# Jupyter notebooks (not tracked per team preference)
*.ipynb

# Planning doc (kept local; README is the shared reference)
plan.md
*.DS_Store